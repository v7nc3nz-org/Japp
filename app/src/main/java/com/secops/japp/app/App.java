/*
 * This source file was generated by the Gradle 'init' task
 */
package com.secops.japp.app;

import com.secops.japp.list.LinkedList;

import static com.secops.japp.utilities.StringUtils.join;
import static com.secops.japp.utilities.StringUtils.split;
import static com.secops.japp.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
